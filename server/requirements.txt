# Pinned ranges so a rebuild months from now matches the code (bug report L2).
# google-genai in particular changes its API surface frequently, and Gemini 3
# models need thought-signature handling that older versions lack.
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.28.0,<1.0.0
anthropic>=0.40.0,<1.0.0
google-genai>=1.0.0,<3.0.0
pydantic>=2.6.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0
